{
  "name": "@mapdesigner/server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "mapdesigner": "./dist/apps/server/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/apps/server/src/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^8.2.0",
    "@mapdesigner/map-core": "workspace:*",
    "@mapdesigner/map-render": "workspace:*",
    "fastify": "^5.6.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
