{
  "name": "mapdesigner",
  "private": true,
  "version": "0.2.0",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.23.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter @mapdesigner/web dev",
    "dev:server": "pnpm --filter @mapdesigner/server dev",
    "start": "pnpm --filter @mapdesigner/server start",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
